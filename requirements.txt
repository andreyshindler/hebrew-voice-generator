# Runtime dependencies. Regenerate with: pip freeze | grep -iE 'edge-tts|fastapi|uvicorn|jinja2|multipart|starlette|pydantic|aiohttp|anyio'
edge-tts>=7.2,<8
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9
# Declared rather than inherited: edge-tts happens to pull it in, and both the
# renderer client and the transcription client would break silently if that
# ever stopped being true.
aiohttp>=3.9
